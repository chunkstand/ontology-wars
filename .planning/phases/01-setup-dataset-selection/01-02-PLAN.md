---
phase: 01-setup-dataset-selection
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [ontology-wars.jsx]
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, UI-02]

must_haves:
  truths:
    - "Datasets display in responsive card grid (CSS Grid auto-fit)"
    - "Each card shows tier badge with icon (star/shield/trophy)"
    - "Card displays name, agency, tier, description, sample query"
    - "Click anywhere on card selects exactly one dataset"
    - "Selected card has visual highlight (border/glow)"
  artifacts:
    - path: "ontology-wars.jsx"
      provides: "Dataset card with tier badges and selection"
      contains: "DatasetCard component"
    - path: "ontology-wars.jsx"
      provides: "Responsive grid layout for datasets"
      contains: "DatasetStep component with CSS Grid"
  key_links:
    - from: "DatasetStep"
      to: "TournamentContext"
      via: "setSelectedDataset"
      pattern: "setSelectedDataset.*dataset"
---

<objective>
Build the Dataset Selection step of the wizard. Displays curated datasets in a responsive card grid with tier badges (star/shield/trophy), allowing single selection.
</objective>

<tasks>

<task type="auto">
  <name>Create TierBadge component</name>
  <files>ontology-wars.jsx</files>
  <action>
Create TierBadge component: receives tier (1/2/3), renders icon + number. Tier 1: Star (★), Tier 2: Shield (⬡), Tier 3: Trophy (🏆). Use distinct colors per tier (gold/orange/red). Position absolute top-right of card.
  </action>
  <verify>grep -n "TierBadge" ontology-wars.jsx</verify>
  <done>TierBadge displays tier icon and number</done>
</task>

<task type="auto">
  <name>Create DatasetCard component</name>
  <files>ontology-wars.jsx</files>
  <action>
Create DatasetCard component: receives dataset object and selected boolean. Displays: name, tier badge (via TierBadge), agency, short description, sample query excerpt. Click anywhere calls onSelect. Selected state: highlighted border with accent color glow.
  </action>
  <verify>grep -n "DatasetCard" ontology-wars.jsx</verify>
  <done>DatasetCard renders all dataset info with click-to-select</done>
</task>

<task type="auto">
  <name>Create DatasetStep wizard component</name>
  <files>ontology-wars.jsx</files>
  <action>
Create DatasetStep component: uses useTournament hook, maps over datasets array, renders CSS Grid with auto-fit minmax(300px, 1fr), each cell is DatasetCard. Include section header "SELECT DATASET". Show "Continue" button enabled only when dataset selected (navigates to step 2).
  </action>
  <verify>grep -n "DatasetStep" ontology-wars.jsx</verify>
  <done>DatasetStep shows grid of selectable dataset cards</done>
</task>

</tasks>

<verification>
Card grid displays datasets with tier badges, single selection works, grid is responsive.
</verification>

<success_criteria>
- [ ] Datasets render in responsive card grid
- [ ] Each card shows tier badge with correct icon
- [ ] Click anywhere on card selects it (single selection)
- [ ] Selected card has visual highlight
- [ ] Continue button enabled when dataset selected
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-dataset-selection/01-02-SUMMARY.md`
</output>
