---
phase: 01-setup-dataset-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ontology-wars.jsx]
autonomous: true
requirements: [UI-02, SEC-01]

must_haves:
  truths:
    - "Setup wizard has 4 steps: Dataset, Agents, Review, Launch"
    - "Sidebar shows numbered steps and current step indicator"
    - "API keys are stored in React state only, never to localStorage/sessionStorage"
    - "TournamentContext provides session-only state to all wizard steps"
  artifacts:
    - path: "ontology-wars.jsx"
      provides: "Setup wizard container with step state and navigation"
      contains: "SetupWizard component"
    - path: "ontology-wars.jsx"
      provides: "Session-only context for tournament configuration"
      contains: "TournamentContext with useState"
  key_links:
    - from: "SetupWizard"
      to: "TournamentContext"
      via: "useTournament hook"
      pattern: "useContext.*TournamentContext"
---

<objective>
Create the setup wizard foundation with session-only state management. This plan establishes the 4-step wizard structure (Dataset → Agents → Review → Launch) and the TournamentContext that holds all configuration in memory only.
</objective>

<tasks>

<task type="auto">
  <name>Create TournamentContext with session-only state</name>
  <files>ontology-wars.jsx</files>
  <action>
Add TournamentContext using React createContext/useContext. Store: selectedDataset, agents (4 house agents: Ontomancer, AE_Sentinel, Graphwright, Dataminer with icons/colors), agentApiKeys, agentPromptSuffixes, webhookUrl, scoringWeights. Use useState only - NO localStorage/sessionStorage. Export useTournament hook.
  </action>
  <verify>grep -n "TournamentContext" ontology-wars.jsx</verify>
  <done>TournamentContext provides session-only state accessible via useTournament hook</done>
</task>

<task type="auto">
  <name>Create SetupWizard container with 4-step navigation</name>
  <files>ontology-wars.jsx</files>
  <action>
Create SetupWizard component that: (1) Uses step state (0-3) for Dataset/Agents/Review/Launch steps, (2) Renders StepSidebar with numbered steps and current indicator, (3) Conditionally renders step component based on currentStep, (4) Provides next/back navigation. Replace current LobbyScreen usage with SetupWizard in main app routing.
  </action>
  <verify>grep -n "SetupWizard" ontology-wars.jsx</verify>
  <done>Wizard navigates through 4 steps with sidebar indicator</done>
</task>

<task type="auto">
  <name>Create StepSidebar component</name>
  <files>ontology-wars.jsx</files>
  <action>
Create StepSidebar component: receives currentStep and onNavigate callback, renders numbered steps (1: Dataset, 2: Agents, 3: Review, 4: Launch), highlights current step, click to navigate (only to completed steps or next step). Style: vertical sidebar with step numbers, labels, connecting line.
  </action>
  <verify>grep -n "StepSidebar" ontology-wars.jsx</verify>
  <done>StepSidebar displays 4 steps with visual current indicator</done>
</task>

</tasks>

<verification>
Wizard renders with 4 steps, context provides session-only state, sidebar navigates between steps.
</verification>

<success_criteria>
- [ ] TournamentContext exists with all session state (no localStorage/sessionStorage)
- [ ] SetupWizard renders 4-step flow
- [ ] StepSidebar shows progress and allows navigation
- [ ] All state clears on page refresh (session-only)
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-dataset-selection/01-01-SUMMARY.md`
</output>
