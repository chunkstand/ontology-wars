---
phase: 01-setup-dataset-selection
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified: [ontology-wars.jsx]
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "Review step shows tournament preview (dataset + configured agents)"
    - "Launch button enabled when: Dataset selected + 2+ agents configured"
    - "Click Launch transitions to Arena screen with configured agents"
    - "Title screen links to Setup, Arena, Leaderboard, API Docs"
  artifacts:
    - path: "ontology-wars.jsx"
      provides: "Review and launch step"
      contains: "ReviewStep component"
    - path: "ontology-wars.jsx"
      provides: "Title screen with navigation"
      contains: "TitleScreen with nav links"
  key_links:
    - from: "ReviewStep"
      to: "Arena"
      via: "startBattle with agent config"
      pattern: "setScreen.*arena"
---

<objective>
Create the Review step and finalize Launch functionality. Show tournament preview, enable launch when ready, and ensure Title screen has proper navigation to all screens.
</objective>

<tasks>

<task type="auto">
  <name>Create ReviewStep wizard component</name>
  <files>ontology-wars.jsx</files>
  <action>
Create ReviewStep component: uses useTournament, displays selected dataset summary (name, tier, agency), displays configured agents list with name/icon/provider/model. Shows tournament bracket preview (semifinals → final visual). "Launch" button: enabled when selectedDataset && agents.filter(a => a.apiKey).length >= 2. Calls startBattle with agent config then setScreen("arena"). "Back" button navigates to step 2.
  </action>
  <verify>grep -n "ReviewStep" ontology-wars.jsx</verify>
  <done>ReviewStep shows tournament preview and Launch button</done>
</task>

<task type="auto">
  <name>Update TitleScreen with navigation</name>
  <files>ontology-wars.jsx</files>
  <action>
Enhance TitleScreen: add navigation links/buttons for Setup (→ wizard), Arena (→ lobby/arena), Leaderboard (→ leaderboard), API Docs (→ placeholder or console log for now). Existing ENTER ARENA button → goes to Setup wizard (step 1). Keep particle canvas effect.
  </action>
  <verify>grep -n "TitleScreen" ontology-wars.jsx</verify>
  <done>TitleScreen navigates to Setup, Arena, Leaderboard, API Docs</done>
</task>

<task type="auto">
  <name>Wire Launch to Arena with agent configuration</name>
<files>ontology-wars.jsx</files>
  <action>
Update startBattle callback to: (1) Get agents from TournamentContext (not hardcoded AGENT_PERSONAS), (2) Merge agent config (provider, model, apiKey, promptSuffix) into persona, (3) Pass configured agents to arena. Ensure API keys from session state are used for actual LLM calls (currently hardcoded - this prepares for real API key usage).
  </action>
  <verify>grep -n "startBattle" ontology-wars.jsx</verify>
  <done>Launch passes configured agents to arena</done>
</task>

</tasks>

<verification>
Review shows preview, Launch enabled when ready, Title screen has all navigation links.
</verification>

<success_criteria>
- [ ] ReviewStep displays dataset + agent preview
- [ ] Tournament bracket visual shown
- [ ] Launch enabled when dataset + 2+ agents configured
- [ ] Title screen links to Setup, Arena, Leaderboard, API Docs
- [ ] Launch transitions to Arena with configured agents
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-dataset-selection/01-04-SUMMARY.md`
</output>
