---
phase: 01-setup-dataset-selection
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [ontology-wars.jsx]
autonomous: true
requirements: [AGENT-01, AGENT-02, AGENT-03, SEC-01]

must_haves:
  truths:
    - "Four house agent slots display with name, icon, color"
    - "Password-style API key input with Show/Hide toggle"
    - "+ card opens modal to add/configure agent"
    - "Click existing agent card opens modal to configure"
    - "Modal has provider/model dropdowns (Anthropic, OpenAI, Google)"
    - "Optional system prompt suffix text field"
    - "API keys stored ONLY in React state (session-only)"
  artifacts:
    - path: "ontology-wars.jsx"
      provides: "Agent slot cards and configuration modal"
      contains: "AgentCard, AgentModal components"
    - path: "ontology-wars.jsx"
      provides: "Password input with show toggle"
      contains: "PasswordInput component"
    - path: "ontology-wars.jsx"
      provides: "Agent configuration step"
      contains: "AgentsStep component"
  key_links:
    - from: "AgentsStep"
      to: "TournamentContext"
      via: "agentApiKeys, setAgentApiKeys"
      pattern: "setAgentApiKeys"
---

<objective>
Build the Agent Configuration step of the wizard. Four house agents with editable provider/model, password-style API key input, and optional prompt suffixes. All keys stored in session-only React state.
</objective>

<tasks>

<task type="auto">
  <name>Create PasswordInput component</name>
  <files>ontology-wars.jsx</files>
  <action>
Create PasswordInput component: receives value, onChange, placeholder. Uses useState for show/hide toggle. Renders input with type={show ? 'text' : 'password'}, lock icon, "Show/Hide" button. Include "Keys stored in session only" helper text below.
  </action>
  <verify>grep -n "PasswordInput" ontology-wars.jsx</verify>
  <done>PasswordInput shows/hides API key with toggle</done>
</task>

<task type="auto">
  <name>Create AgentCard component for slots</name>
  <files>ontology-wars.jsx</files>
  <action>
Create AgentCard component: receives agent object (id, name, icon, color, provider, model, apiKey, promptSuffix), selected boolean, onClick. Displays: icon, name, provider badge if configured. Click opens config modal. House agents: Ontomancer (⬡, purple), AE_Sentinel (⬟, green), Graphwright (⬢, orange), Dataminer (◉, cyan).
  </action>
  <verify>grep -n "AgentCard" ontology-wars.jsx</verify>
  <done>AgentCard displays agent with icon, name, provider</done>
</task>

<task type="auto">
  <name>Create AgentModal with native dialog</name>
  <files>ontology-wars.jsx</files>
  <action>
Create AgentModal component: uses native HTML <dialog>, receives isOpen, agent, onClose, onSave. Fields: Name (text), Icon picker (preset icons), Color picker (preset colors), Provider dropdown (Anthropic, OpenAI, Google), Model dropdown (filtered by provider), API Key (via PasswordInput), Prompt Suffix (textarea, optional). "Save" button calls onSave with form data. "Cancel" closes.
  </action>
  <verify>grep -n "AgentModal" ontology-wars.jsx</verify>
  <done>AgentModal configures agent via native dialog</done>
</task>

<task type="auto">
  <name>Create AgentsStep wizard component</name>
  <files>ontology-wars.jsx</files>
  <action>
Create AgentsStep component: uses useTournament, renders 4 agent slots in 2x2 grid. Each slot is AgentCard. Click card opens AgentModal. "+" card adds new slot (if &lt; 4 total). Include section header "CONFIGURE AGENTS". Show "Continue" button enabled when 2+ agents have API keys configured. Show "Back" button.
  </action>
  <verify>grep -n "AgentsStep" ontology-wars.jsx</verify>
  <done>AgentsStep shows 4 configurable agent slots</done>
</task>

</tasks>

<verification>
Agent cards display with icons/colors, modal configures provider/model/API key, password input toggles visibility, 2+ agents required to continue.
</verification>

<success_criteria>
- [ ] Four house agents display with icons and colors
- [ ] Click agent opens modal for configuration
- [ ] Provider/model dropdowns work (Anthropic, OpenAI, Google)
- [ ] PasswordInput toggles show/hide
- [ ] API keys stored ONLY in React state
- [ ] Continue enabled when 2+ agents configured
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-dataset-selection/01-03-SUMMARY.md`
</output>
